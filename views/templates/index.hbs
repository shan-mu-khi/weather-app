<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/styles/styles.css">
    </head>
    <body>
        {{>header}}
        <h1>{{title}}</h1>
        <p class="finalDiv">Use this website to get the weather!!</p>
        <form>
            <div class="form_container">
            <input id="location" autocomplete="off" placeholder="Enter location">
            <button class="submit">Submit</button>
            <div class="result"></div>
            </div>
        </form>
        {{>footer}}
       <script src="/js/app.js"></script>

    </body>
</html>